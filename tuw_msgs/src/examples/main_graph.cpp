#include <iostream>

int main(int argc, char ** argv)
{
  (void)argc;
  (void)argv;
  /*
  std::string filename_in = "res/graph.txt";
  std::cout << "usage: " << argv[0] << " <filename>" << std::endl;
  std::cout << "default filname = " << filename_in << std::endl;
  if (argc == 2) {filename_in = argv[1];}
  std::cout << "reading filname = " << filename_in << std::endl;

  tuw_msgs::Graph graph_serialization;
  tuw_graph_msgs::msg::Graph msg;
  graph_serialization.read(filename_in, msg);

  std::string filename_out = filename_in + ".out.txt";
  std::cout << "writing filname = " << filename_out << std::endl;
  graph_serialization.write(filename_out, msg);
  */
}
